# AI Agentic Assistant V2 — requirements
# Last updated: 2026-06-06 — nemoguardrails removed (langchain conflict)
# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.12
slowapi==0.1.9

# LangGraph / LangChain
langgraph==0.2.56
langchain==0.3.13
langchain-core==0.3.28
langchain-openai==0.2.14
langchain-community==0.3.13
langsmith==0.2.3

# OpenAI
openai>=1.58.1,<2.0.0

# Pydantic / Settings
pydantic==2.10.4
pydantic-settings==2.7.0

# Vector stores
faiss-cpu==1.9.0.post1
chromadb==0.5.23

# Embeddings / RAG
sentence-transformers==3.3.1
flashrank==0.2.9

# Document parsing
pypdf==5.1.0
unstructured==0.16.10

# Graph database
neo4j==5.27.0
tenacity==9.0.0

# Redis (async)
redis==5.2.1

# Observability
mlflow==2.19.0
prometheus-client==0.21.1

# Ollama
ollama==0.4.4

# Web search
tavily-python==0.5.0

# Image handling
Pillow==11.1.0

# Auth
python-jose[cryptography]==3.3.0

# Code execution sandbox
RestrictedPython>=7.0

# Async / utils
httpx>=0.27.0,<0.28.0
uvloop==0.21.0; sys_platform != "win32"
h11==0.14.0


# Phase A: Memory
mem0ai==0.1.29
pgvector==0.3.2
psycopg2-binary==2.9.9
asyncpg==0.29.0 

# Phase D: Voice
faster-whisper==1.0.3
soundfile==0.12.1
numpy==1.26.4
# TTS via Docker (Coqui) — not installed in backend directly
# Optional: pip install TTS if running Coqui in same process  


# Phase E: Guardrails
# nemoguardrails removed — conflicts with langchain>=0.3.0
# guard_engine.py degrades gracefully when not installed (try/except ImportError)
spacy==3.7.4
# Download spaCy model after install:
# python -m spacy download en_core_web_sm   


# Phase H: Auth + RBAC
passlib[bcrypt]==1.7.4
# python-jose already listed above (line 55) — duplicate removed

# ── Phase N: Guardian Agent (Compliance) ─────────────────────────────────────
presidio-analyzer==2.2.355          # Microsoft Presidio NLP PII/PHI
presidio-anonymizer==2.2.355        # Presidio masking engine
# spaCy model required: python -m spacy download en_core_web_lg

# ── Phase O: HITL + Notifications ────────────────────────────────────────────
# sendgrid — email notifications (SENDGRID_API_KEY)
sendgrid==6.11.0
# httpx already installed above for outbound HTTP (Slack webhook)

# ── Features 4–19: Shared dependencies ───────────────────────────────────────
# Task Scheduler (Feature 4)
apscheduler==3.10.4
celery[redis]==5.4.0

# Commerce & Payments (Features 5, 8)
stripe==11.3.0
razorpay==1.4.2
twilio==9.3.7

# Multi-modal Output (Feature 7)
reportlab==4.2.5
python-pptx==1.0.2
openpyxl==3.1.5

# India Form Reader (Feature 14)
# GPT-4o Vision used — no extra package needed beyond openai

# Email OAuth (Feature 15)
google-auth==2.37.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.155.0
msal==1.31.1                        # Microsoft Azure / Outlook

# CRM Integration (Feature 16)
hubspot-api-client==11.1.0

# Social Media (Feature 19)
# Buffer & Twitter REST via httpx — no heavy SDK needed
tweepy==4.15.0                      # Twitter/X API v2

# A/B Testing statistics (Feature 12)
scipy==1.14.1                       # Welch's t-test p-values (pure-Python fallback exists without this)